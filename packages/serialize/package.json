{
  "name": "@uw/serialize",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "模型 → docx：补丁式回写，没改的部件与节点原样吐回",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@uw/core": "workspace:*",
    "@uw/model": "workspace:*",
    "@uw/ooxml": "workspace:*"
  },
  "devDependencies": {
    "fflate": "^0.8.3",
    "tsdown": "^0.22.14"
  }
}
